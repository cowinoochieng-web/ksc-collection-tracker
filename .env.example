# Copy this file to .env and fill in the API key/secret from the
# dedicated "KSC API Reader" ERPNext user to enable the live /integration
# page. Without these set, the app runs fine and shows mock ERPNext data.
ERPNEXT_URL=http://ksc.localhost:8000
ERPNEXT_API_KEY=
ERPNEXT_API_SECRET=
